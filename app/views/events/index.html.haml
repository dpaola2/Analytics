%h3 Events
.span8
  %table.table.table-condensed
    %thead
      %tr
        %th when
        %th Blob
    %tbody
      - @events.each do |event|
        %tr
          %td= time_ago_in_words(event.created_at)
          %td= event.blob
  = will_paginate @events
